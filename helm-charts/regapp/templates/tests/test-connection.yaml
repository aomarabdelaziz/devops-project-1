apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "regapp.fullname" . }}-test-connection"
  labels:
    {{- include "regapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "regapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
