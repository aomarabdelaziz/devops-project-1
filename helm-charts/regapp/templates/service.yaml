apiVersion: v1
kind: Service
metadata:
  name: {{ include "regapp.fullname" . }}
  labels:
    {{- include "regapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "regapp.selectorLabels" . | nindent 4 }}
